/// Actions that observers can take during golden section search.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum Action {
    // TODO: Define actions (e.g., StopEarly, AssumeWorse, AssumeBetter)
}
